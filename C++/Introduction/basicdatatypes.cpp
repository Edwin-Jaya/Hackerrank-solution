#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int i;
    long ld;
    char ch;
    float f;
    double d;
    scanf("%d %ld %c %f %lf", &i, &ld, &ch, &f, &d);
    printf("%d\n%ld\n%c\n%f\n%lf", i, ld, ch, f, d);
       
    return 0;
}
